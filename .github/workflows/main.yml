name: Test Action

on:
  push:
    branches:
      - main  # or any branch you want to trigger the action

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: "21f2000690@ds.study.iitm.ac.in"  # Fix: Wrapped in quotes
        run: echo "Hello, world!"
